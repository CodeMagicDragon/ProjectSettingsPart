<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="StandartButton" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/buttonColor</item>
        <item name="colorControlHighlight">@color/backgroundColor</item>
    </style>
    <style name="CardButton" parent="@style/Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#ffffff</item>
        <item name="colorControlHighlight">#bdffac</item>
    </style>

</resources>
